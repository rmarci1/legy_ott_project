import {User} from "../../../Types/User.ts";

interface profileModalProps {
    user: User,
    setModal: (value: boolean) => void,
    setJobModal: (value: boolean) => void
}

export default function ProfileModal({user, setModal, setJobModal}: profileModalProps){

    return<>
    </>
}